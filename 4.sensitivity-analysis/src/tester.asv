%Same input as ass.1

%price = 11;
strike = 12;
rate = 0.02;
time = 82/252;
volatility = 0.12;
yield = 0;

priceVector =[0:15];
for i=1:length(priceVector);
    price = i;
    [a,b] = blackScholes(i, strike, rate, time, volatility, yield);
    
end
    
[a,b] = blackScholes(price, strike, rate, time, volatility, yield);
price = 12;
[a1,b1] = blackScholes(price, strike, rate, time, volatility, yield);
